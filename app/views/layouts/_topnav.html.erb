<nav class="navbar navbar-light navbar-expand-md navigation-clean">
  <div class="container position-relative">
    <a class="navbar-brand" href="<%= root_path %>">Mulka</a
    ><button data-toggle="collapse" data-target="#top-nav" class="navbar-toggler border-0">
      <span class="sr-only">Toggle navigation</span
      ><span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse bg-white" id="top-nav">
      <ul class="nav navbar-nav ml-auto">
        <li role="presentation" class="nav-item">
          <%= link_to 'Property', properties_path, class:"nav-link py-3 py-md-2" %>
        </li>
        <% if @current_user.blank? %>
          <li role="presentation" class="nav-item">
            <%= link_to 'Login', login_path, class: "nav-link py-3 py-md-2" %>

          </li>
        <% else %>
          <li class="nav-item dropdown show">
            <%= link_to "#{@current_user.first_name} #{@current_user.last_name}", '#', class:"dropdown-toggle nav-link py-3 py-md-2", 'data-toggle': 'dropdown', 'aria-expanded': 'true' %>

            <div role="menu" class="dropdown-menu dropdown-menu-right">
              <% if @current_user.user_type == 'user' %>
                <%= render partial: 'layouts/usermenu' %>
              <% else %>
                <%= render partial: 'layouts/adminmenu' %>
              <% end %>

              <%= link_to login_path, method: 'delete', class:'dropdown-item py-3 pl-3' do %>
                <i class="fas fa-sign-out-alt d-none d-md-inline text-info mr-3"></i>Sign Out
              <% end %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
